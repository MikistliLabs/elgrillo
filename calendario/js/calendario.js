// Lógica principal del calendario (optimizado)
document.addEventListener('DOMContentLoaded', function() {
    // Aquí va tu lógica de calendario optimizada
    // Puedes integrar eventos, navegación de meses, etc.
    // Ejemplo de disparo de evento personalizado:
    // window.dispatchEvent(new CustomEvent('calendarEventClick', { detail: { title: 'Evento', date: '2025-05-01' } }));
});
